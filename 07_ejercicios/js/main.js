//---------EJERCICIO 2----------
//Crear un programa que permita detectar si una frase finaliza con punto.

/* function finConPunto(frase) {
    return frase
}
let result = finConPunto(prompt("Ingresa una frase."))
let letra = result.slice(-1)

if (letra === ".") {
    console.log("¡Muy bien! Finalizaste con punto")
} else {
    console.log("Ups! Parece que olvidaste el punto")
} */

var names = ["Hole-in-one!", "Eagle", "Birdie", "Par", "Bogey", "Double Bogey", "Go Home!"];
function golfScore(par, strokes) {
  // Cambia solo el código debajo de esta línea


  return "Change Me";
  // Cambia solo el código encima de esta línea
}

golfScore(5, 4);





